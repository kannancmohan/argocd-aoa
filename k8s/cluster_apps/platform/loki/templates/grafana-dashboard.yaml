---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-dashboard
  labels:
    grafana_dashboard: "1"
data:
  dashboard.json: |
    {{ .Files.Get "grafana-dashboards/loki-grafana-dashboard.json" | indent 4 }}
