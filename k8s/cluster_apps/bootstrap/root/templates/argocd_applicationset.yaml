{{- range $index, $appSet := .Values.applicationSet }}
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: {{ $appSet.name }}
  namespace: {{ $.Release.Namespace }}
spec:
  generators:
    - git:
        repoURL: {{ $.Values.gitops.repo }}
        revision: {{ $.Values.gitops.revision }}
        directories:
          - path: {{ $appSet.path }}/*
  template:
    metadata:
      name: '{{ `{{path.basename}}` }}' # path.basename on "/directory/directory2" would produce "directory2"
    spec:
      destination:
        name: in-cluster
        namespace: '{{ default `{{path.basename}}` $appSet.namespace }}'
      project: '{{ default "default" $appSet.project }}'
      source:
        # helm:
        #   valueFiles:
        #     - $values/k8s/cluster_apps/apps-values.yaml
        #     - values.yaml
        repoURL: {{ $.Values.gitops.repo }}
        path: '{{ `{{path}}` }}'
        targetRevision: {{ $.Values.gitops.revision }}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
          # allow_empty: true
        retry:
          limit: 10
          backoff:
            duration: 1m
            factor: 2
            maxDuration: 16m
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
{{- end }}
